@model IEnumerable<TruyenChu.Data.Role>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<div>
    <a class="btn btn-primary" asp-area="Admin" asp-controller="Roles" asp-action="Create"><i class="mdi mdi-plus"></i>Create New</a>
</div>
<hr />
<table class="table table-bordered">
    <thead>
        <tr class="table-warning">
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="table-success">
                <td>
                    @Html.DisplayFor(modelItem => item.RoleName)
                </td>
                <td>
                    <a asp-area="Admin" asp-controller="Roles" asp-action="Edit" asp-route-id="@item.RoleId">Edit</a> |
                    <a asp-area="Admin" asp-controller="Roles" asp-action="Details" asp-route-id="@item.RoleId">Details</a> |
                    <a asp-area="Admin" asp-controller="Roles" asp-action="Delete" asp-route-id="@item.RoleId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

